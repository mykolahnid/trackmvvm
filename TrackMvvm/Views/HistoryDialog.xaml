<Window x:Class="TrackMvvm.Views.HistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TrackMvvm.Views"
        xmlns:xcdg="http://schemas.xceed.com/wpf/xaml/datagrid"
        xmlns:viewModel="clr-namespace:TrackMvvm.ViewModel"
        mc:Ignorable="d"
        Title="History" Height="303.178" Width="538.771">

    <Window.DataContext>
        <viewModel:WorkSessionHistoryViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <DataGrid SelectionUnit="Cell" Grid.Row="0" ItemsSource="{Binding History}" AutoGenerateColumns="True" AutoGeneratedColumns="DataGrid_OnAutoGeneratedColumns"></DataGrid>
        <Label HorizontalAlignment="Right" Grid.Row="1">Would you like to delete history?</Label>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Margin="10" Padding="30,5" Click="Button_Click" IsDefault="True">Yes</Button>
            <Button Grid.Column="2" Margin="10" Padding="30,5" IsCancel="True" Click="Button_Click_1">No</Button>
        </Grid>

    </Grid>
</Window>
